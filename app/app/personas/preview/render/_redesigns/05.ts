// @ts-nocheck
// Persona-review snapshot retired: this redesign targeted the old
// /batches/preview exploration variants, which were removed when the
// settlement explorer was ported to the design kit. The snapshot under
// tools/persona-review/runs/05 still depended on that pruned module, so the
// wrapper now renders an inert placeholder rather than importing dead code.
export { RetiredSnapshot as default } from "./_retired";
