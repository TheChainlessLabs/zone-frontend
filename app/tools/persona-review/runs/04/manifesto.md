A treasury committee does not read marketing copy. It reads the order ticket, the audit row, and the timestamp. Omega already has the right vocabulary — sealed, proof, attestation, midpoint — but renders it in retail-DEX clothing: chip pills, percentage shortcuts, tinted Buy buttons, decorative donuts, italic mood lines. None of that survives a private-banking review. The /batches detail page is the one surface that already reads like infrastructure; carry that register everywhere else. Open every ticket with a Marquee-style contract row. Print UTC on every primary surface. Make every hash a live link. Replace tinted celebration CTAs with a neutral primary action that restates the full order. Spend the serif once, as gravity, never as decoration. Private capital needs private tone.
