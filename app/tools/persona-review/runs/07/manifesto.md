`/batches` should feel like a thin shell over settlement state, not a brand moment. Drop the decorative background, compress the rows into a real table, keep batch selection and proof evidence in one desktop working set, and make direct lookup faster than browsing. GitHub, Grafana, Stripe, CloudWatch, Postman, iTerm2: same lesson every time. Operators trust surfaces that expose the object, the filter, the failure, and the next hop without theatrics. If I have to admire the page before I can debug the batch, the page is lying about its job.
