If I cannot finish a USDC/EURC buy with my right thumb on a 6.1" Android while standing on a Grab, Omega does not exist for me — and I am 600 million SEA users wide. The current trade page is a desktop form shrunk into a phone column, the bottom tab bar overlaps the primary CTA on every page, the amount entry is a tiny text box that the OS keyboard immediately covers, and PairSwitcher steals a row that should live in the header. Build mobile-first: sticky CTA above the safe-area inset, inline numeric keypad as the amount surface, pair into the header pill, bottom-sheet confirm everywhere, one segmented control, not three. Phone first, always — desktop is a courtesy.
