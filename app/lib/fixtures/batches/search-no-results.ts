import type { BatchesSearchFixture } from "../types";

/** /batches search — query returned nothing. */
export const batchesSearchNoResults: BatchesSearchFixture = {
  query: "0xdead",
  results: [],
};
