// @ts-nocheck
// The persona-18 portfolio redesign was generated against the
// /portfolio/preview exploration scaffolding (`_variants/_shared`), removed
// when the portfolio surface was ported to the design kit. Falls back to the
// archived placeholder so the persona-review render route still builds; re-run
// the persona pipeline to regenerate a live redesign.
export { default } from "./_archived";
